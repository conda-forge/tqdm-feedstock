{% set version = "4.7.4" %}

package:
  name: tqdm
  version: {{ version }}

source:
  fn: tqdm-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/t/tqdm/tqdm-{{ version }}.tar.gz
  sha256: b0cd085c6285780127c2d8eb50b0f3b067d5a221ed6c11bc5965cf83bc51b0b3

build:
  entry_points:
    - tqdm=tqdm._main:main

  number: 0
  script: python setup.py install --single-version-externally-managed --record=record.txt

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  imports:
    - tqdm

  commands:
    - tqdm --help
    - tqdm -v

about:
  home: https://github.com/tqdm/tqdm
  license: Mozilla Public License 2.0 (MPL 2.0) or MIT License
  summary: 'A Fast, Extensible Progress Meter'

extra:
  recipe-maintainers:
    - pmlandwehr
    - ericdill
